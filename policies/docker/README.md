# Docker policies

Runtime isolation rules cover privileged mode, Docker socket access, host PID, and host networking. Future capability/mount/rootfs rules require workload-intent evidence to avoid generic checklist output.

